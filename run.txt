1. go to frontend folder, and run npm install --legacy-peer-deps, after this run npm start to start the project
2. go to backend folder, and make sure you have docker installed upfront, then simply run docker-compose up
